# Lab Content Files

Each file here is a lab. The naming convention is:
  {track}-{topic}-{name}.tsx

Example:
  web-sqli-basic-injection.tsx
  linux-privesc-suid.tsx

The file ID in Firebase should match the filename without .tsx extension.
